

//06. 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon' split the string at the comma and change it to an array.

let myStr = 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon';

let ansStr = myStr.split(",");

console.log(ansStr)

console.log("length of splited string is " , ansStr.length)